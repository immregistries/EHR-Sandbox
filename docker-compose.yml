services:
  #  ehr-sandbox-mysql:
  #    image: mysql:latest
  #    container_name: ehr-sandbox-mysql
  #    command:
  #      --lower_case_table_names=1
  #    restart: unless-stopped
  #    # Healthcheck allows for Ehr-sandbox to wait for the database to be up and running before deploying,  as a connection is necessary for the hibernate configuration to be a success
  #    healthcheck:
  #      test: [ "CMD", "curl", "-f", "http://localhost:3306/ehr" ]
  #      interval: 5s
  #      timeout: 5s
  #      retries: 5
  #      start_period: 5s
  #    environment:
  #      #      this password is not used in app, just changing default password
  #      MYSQL_ROOT_PASSWORD: ${ENV_MYSQL_ROOT_PASSWORD}
  #    volumes:
  #      - ./database/create_database.sql:/docker-entrypoint-initdb.d/1.sql
  #      - ./database/create_user.sql:/docker-entrypoint-initdb.d/2.sql
  ehr-sandbox:
    build: .
    container_name: ehr-sandbox-squala
    env_file:
      - .env
    environment:
      #      - SPRING_DATASOURCE_URL=jdbc:mysql://ehr-sandbox-mysql:3306/ehr
      - SPRING_DATASOURCE_URL=jdbc:h2:file:/data-h2/ehr;
      - ENV_DEFAULT_IIS_URL=http://host.docker.internal:8080/iis
    restart: unless-stopped
    #    volumes:
    #      - ehr-h2-data:/data-h2
    ports:
      #     Default port is 9091, can be changed here
      - "9091:8080"
#    depends_on:
#      ehr-sandbox-mysql:
#        condition: service_healthy
volumes:
  #  sandbox-mysql:
  ehr-h2-data: