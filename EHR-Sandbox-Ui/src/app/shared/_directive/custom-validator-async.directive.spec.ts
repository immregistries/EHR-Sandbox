import { CustomValidatorAsyncDirective } from './custom-validator-async.directive';

describe('CustomValidatorAsyncDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValidatorAsyncDirective();
    expect(directive).toBeTruthy();
  });
});
