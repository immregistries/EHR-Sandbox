import { CustomCalidatorDirective } from './custom-validator.directive';

describe('CustomCalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomCalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
