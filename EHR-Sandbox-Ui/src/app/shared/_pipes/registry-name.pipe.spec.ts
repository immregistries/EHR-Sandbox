import { RegistryNamePipe } from './registry-name.pipe';

describe('RegistryNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RegistryNamePipe();
    expect(pipe).toBeTruthy();
  });
});
